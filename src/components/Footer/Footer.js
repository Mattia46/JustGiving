import React, { Component } from 'react';

const Footer = (props) => (
  <div>
    <a href={props.website}>British Hear Foundation</a>
    <a href={ props.profile }>Just Giving</a>
    </div>
)

export default Footer;
